import { ChangeNewRequestCard } from './change-card-request';

describe('ChangeNewRequestCard', () => {
  it('should create an instance', () => {
    expect(new ChangeNewRequestCard()).toBeTruthy();
  });
});